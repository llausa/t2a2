<% if user_signed_in? %>
  <section class="hero is-info">
    <div class="hero-body">
      <div class="container">
        <h1 class="title has-text-centered">
          Hi <%= current_user.username %>!
        </h1>
        <h1 class="title has-text-centered">
          Welcome back
        </h1>
        <h2 class="subtitle has-text-centered">
          to
        </h2>
        <h2 class="title has-text-centered">
          Objective Bystander
        </h2>
        <h2 class="subtitle has-text-centered">
          your Marketplace for Advice
          <br>from objective bystanders
          <br>on the Internet
        </h2>
      </div>
    </div>
  </section>
<% else %>
    <section class="hero is-info">
    <div class="hero-body">
      <div class="container">
        <h1 class="title has-text-centered">
          Welcome
        </h1>
        <h2 class="subtitle has-text-centered">
          to
        </h2>
        <h2 class="title has-text-centered">
          Objective Bystander
        </h2>
        <h2 class="subtitle has-text-centered">
          a Marketplace for Advice
          <br>from objective bystanders
          <br>on the Internet
        </h2>
      </div>
    </div>
  </section>
<% end %>

<section class="section">
  <h1 class="title has-text-centered">Advice Questions</h1>
  <h2 class="subtitle has-text-centered">
    broken down by topic
  </h2>
  <div class="columns has-text-centered">
    <div class="column">
      <h1 class='title'>
        <%= link_to "#{@general_topic.title}", topic_path(@general_topic) %>
      </h1>
      <% if AdviceQuestion.where(topic_id: @general_topic.id).count > 0 %>
        <% AdviceQuestion.where(topic_id: @general_topic.id)[0..2].each do |q| %>
        <p>
          <%= link_to "#{q.title}", advice_question_path(q.id) %>
        </p>
        <% end %>
      <% else %>
        <p>
          <%= 'Nothing here yet...' %>
        </p>
      <% end %>
    </div>
    <div class="column">
      <h1 class='title'>
        <%= link_to "#{@health_topic.title}", topic_path(@health_topic) %>
      </h1>
      <% if AdviceQuestion.where(topic_id: @health_topic.id).count > 0 %>
        <% AdviceQuestion.where(topic_id: @health_topic.id)[0..2].each do |q| %>
        <p>
          <%= link_to "#{q.title}", advice_question_path(q.id) %>
        </p>
        <% end %>
      <% else %>
        <p>
          <%= 'Nothing here yet...' %>
        </p>
      <% end %>
    </div>
  </div>

  <div class="columns has-text-centered">
    <div class="column">
      <h1 class='title'>
        <%= link_to "#{@wealth_topic.title}", topic_path(@wealth_topic) %>
      </h1>
      <% if AdviceQuestion.where(topic_id: @wealth_topic.id).count > 0 %>
        <% AdviceQuestion.where(topic_id: @wealth_topic.id)[0..2].each do |q| %>
        <p>
          <%= link_to "#{q.title}", advice_question_path(q.id) %>
        </p>
        <% end %>
      <% else %>
        <p>
          <%= 'Nothing here yet...' %>
        </p>
      <% end %>
    </div>
    <div class="column">
      <h1 class='title'>
        <%= link_to "#{@relationships_topic.title}", topic_path(@relationships_topic) %>
      </h1>
      <% if AdviceQuestion.where(topic_id: @relationships_topic.id).count > 0 %>
        <% AdviceQuestion.where(topic_id: @relationships_topic.id)[0..2].each do |q| %>
        <p>
          <%= link_to "#{q.title}", advice_question_path(q.id) %>
        </p>
        <% end %>
      <% else %>
        <p>
          <%= 'Nothing here yet...' %>
        </p>
      <% end %>
    </div>
  </div>
</section>

<% if @advisor_topics %>
  <section class="section">
    <h1 class="title has-text-centered">Advisor Topics</h1>

    <div class="columns has-text-centered">
      <div class="column">
        bruh
      </div>
      <div class="column">
        bruh
      </div>

      <div class="column">
          bruh
      </div>
    </div>

    <div class="columns has-text-centered">
      <div class="column">
        bruh
      </div>
      <div class="column">
        bruh
      </div>
    </div>
  </section>
<% end %>

<hr>
<% if @advisor_topics %>
  <% @advisor_topics[3..-1].each do |topic| %>
    <div>
      <p><%= link_to "#{topic.title} | #{topic.desc}", topic_path(topic) %></p>
      <% if AdviceQuestion.where(topic_id: topic.id) %>
        <% AdviceQuestion.where(topic_id: topic.id).each do |q| %>
          <p><%= '-' + q.title %></p>
        <% end %>
      <% else %>
        <%= 'bruh1' %>
      <% end %>
    </div>
  </div>
  <% end %>
<% end %>